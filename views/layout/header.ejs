<!DOCTYPE html>
<html>
<head>
    <link href="/css/bootstrap.min.css" rel="stylesheet">
    <link href="/css/community-default.css" rel="stylesheet">
    <script type="text/javascript" src="/js/jquery-3.1.1.min.js"></script>

     <!-- Bootstrap Core CSS -->
    <link href="/vendor/bootstrap/css/bootstrap.min.css" rel="stylesheet">

    <!-- Theme CSS -->
    <link href="/css/freelancer.min.css" rel="stylesheet">

    <!-- Custom Fonts -->
    <link href="/vendor/font-awesome/css/font-awesome.min.css" rel="stylesheet" type="text/css">
    <link href="https://fonts.googleapis.com/css?family=Montserrat:400,700" rel="stylesheet" type="text/css">
    <link href="https://fonts.googleapis.com/css?family=Lato:400,700,400italic,700italic" rel="stylesheet" type="text/css">


	<title><%= pagetitle %></title>
</head>
<body style="font-family: Montserrat; font-weight: bold; background-color: #fff; word-wrap: break-word; margin: 0;" id="page-top" class="index">


        <!-- Navigation -->
    <nav id="mainNav" class="navbar navbar-default navbar-fixed-top navbar-custom">
        <div class="container">

            <!-- Brand and toggle get grouped for better mobile display -->
            <div class="navbar-header page-scroll">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
                    <span class="sr-only">Toggle navigation</span> Menu <i class="fa fa-bars"></i>
                </button>
                <a class="navbar-brand" id="a" href="/">Students</a>
            </div>
            <!-- Collect the nav links, forms, and other content for toggling -->
            <!-- <form class="navbar-form navbar-left" method="POST" action="/users/search">
              <div class="form-group">
                <input type="text" class="form-control" name="searchusername" placeholder="search by username">
              </div>
            </form> -->
            <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1" style="font-size: 10px;">
              <ul class="nav navbar-nav navbar">
                <li>
                  <a class="navbar-brand" href="/clubs">Clubs</a>
                </li>
                <li>
                  <a class="navbar-brand" href="/offers">Offers</a>
                </li>
                <li>
                  <a class="navbar-brand" href="/events">Events</a>
                </li>
              </ul>

              <% if(user!=null) { %>
                <div class="collapse navbar-collapse navbar-right">
                  <form class="navbar-form navbar-right" method="GET" action="/users/signout">
                    <div class="form-group">
                      <a class="navbar-nav form-control" href="/users/viewprofile/<%=user.id%>"><%= user.username %></a>
                    </div>
                    <button type="submit" class="btn btn-success">Sign out</button>
                  </form>
                </div>
              <% } else { %>
                <form class="navbar-form navbar-right" method="GET" action="/users/signout">
                  <div class="form-group">
                    <a class="navbar-nav form-control btn-primary" href="/users/signup">Sign Up</a>
                  </div>
                  <div class="form-group">
                    <a class="navbar-nav form-control btn-primary" href="/users/signin">Sign In</a>
                  </div>
                </form> 
              <% } %>
            </div>


            <!-- /.navbar-collapse -->
        </div>
        <!-- /.container-fluid -->
    </nav>
    <div id="backgroundDiv" style="position: fixed; z-index: 8; background-color: black; width: 100%;height: 100%; opacity: 0.95; display: none; margin-top: -50px;"> 
    </div>
<div class="jumbotron container" style="margin-top:90px; background-color: #fff; min-height:100vh; width: 100%; padding: 0; font-family: Montserrat;">

      <div class="row container">

        <div class="col-lg-12" style="margin-left: 25%;" id="alertDiv">
          <% if (success_msg!=null && success_msg!="") { %>
            <div class="alert alert-success"><%= success_msg %></div>
          <% } %>

          <% if (error_msg!=null && error_msg!="") { %>
            <div class="alert alert-danger"><%= error_msg %></div>
          <% } %>

          <% if (error!=null && error!="") { %>
            <div class="alert alert-danger"><%= error %></div>
          <% } %>          
        </div>
      </div>

      <style type="text/css">
        #a:hover {
          color: #18BC9C;
        }
      </style>
      <script type="text/javascript">
      var updateWidth = function() {
        var screenwith = $('body').width();
          if(parseInt(screenwith)<1200) {
            console.log('less')
            $('#tabsBar').hide();
            $('#tabsButton').show();
          } else {
            $('#tabsButton').hide();
            $('#tabsBar').show();
          }
      }
      updateWidth();
        $(window).resize(function() {
          updateWidth();
        });
      </script>
